export default function Navbar(){
    return(
        <nav>
            <a href="#home">Jed Villapando</a>
            <div>
                <a href="#projects">projects</a>
                <a href="#contact">Contact</a>
            </div>
        </nav>
    );
}